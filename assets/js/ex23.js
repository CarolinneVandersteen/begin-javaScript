/*Willekeurig 2

Declareer de volgende variabelen:

Toon de willekeurige waarde tussen 50 en 100 in de console.
Toon de willekeurige waarde tussen 0 of 1 in de console.
Toon de willekeurige waarde (die kan variëren) tussen 0 tot en met 10 inclusief) in de console*/




//oefening 3 math

console.log(Math.floor((Math.random() * 51) + 50));
console.log(Math.floor((Math.random() * 2) + 0));
console.log(Math.floor((Math.random() * 11) + 0));

//Math.floor(Math.random() * (max - min + 1)) + min;

/*Math.ceil(x)	            //Returns the value of x rounded up to its nearest integer
Math.floor(x)	            //Returns the value of x rounded down to its nearest integer
Math.max(x, y, z, ...)	    //Returns the number with the highest value
Math.min(x, y, z, ...)	    //Returns the number with the lowest value
Math.random()	            //Returns a random number between 0 and 1
Math.round(x)	            //Returns the value of x rounded to its nearest integer*/
